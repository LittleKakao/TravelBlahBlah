<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.littlekakao.travelblahblah.journey.mapper.JourneyMapper">

    <select id="selectJourneyAll" resultType="Journey">
        SELECT *
        FROM journey
    </select>

    <select id="selectJourneyByJourneyId" resultType="Journey">
        SELECT *
        FROM journey
        WHERE journey_id = #{journeyId}
    </select>

    <insert id="insertJourney" parameterType="Journey">
        INSERT INTO journey (
                             user_id,
                             journey_name,
                             journey_destination,
                             journey_start_date,
                             journey_end_date,
                             journey_create_date,
                             journey_update_date,
                             journey_delete_yn
        ) VALUE (
            #{userId},
            #{journeyName},
            #{journeyDestination},
            #{journeyStartDate},
            #{journeyEndDate},
            now(),
            now(),
            'N'
            )
    </insert>

    <update id="updateJourneyByJourneyId" parameterType="map">
        UPDATE journey
        SET journey_name = #{journey.journeyName},
            journey_destination = #{journey.journeyDestination},
            journey_start_date = #{journey.journeyStartDate},
            journey_end_date = #{journey.journeyEndDate},
            journey_update_date = now()
        WHERE journey_id = #{journeyId}
    </update>

    <delete id="deleteJourneyByJourneyId" parameterType="int">
        DELETE
        FROM journey
        WHERE journey_id = #{journeyId}
    </delete>


</mapper>