<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.littlekakao.travelblahblah.journey.mapper.JourneyMapper">

    <select id="findJourneyAll" resultType="Journey">
        SELECT *
        FROM journey
    </select>

    <select id="findJourneyByJourneyId" resultType="Journey">
        SELECT *
        FROM journey
        WHERE journey_id = #{journeyId}
    </select>

    <insert id="addJourney" parameterType="Journey" useGeneratedKeys="true" keyProperty="journeyId">
        INSERT INTO journey (
                             user_id,
                             journey_name,
                             journey_destination,
                             journey_start_date,
                             journey_end_date,
                             journey_create_date,
                             journey_update_date,
                             journey_delete_yn
        ) VALUE (
            #{userId},
            #{journeyName},
            #{journeyDestination},
            #{journeyStartDate},
            #{journeyEndDate},
            now(),
            now(),
            'N'
            )
    </insert>

    <update id="modifyJourneyByJourneyId" parameterType="map">
        UPDATE journey
        SET journey_name = #{journey.journeyName},
            journey_destination = #{journey.journeyDestination},
            journey_start_date = #{journey.journeyStartDate},
            journey_end_date = #{journey.journeyEndDate},
            journey_update_date = now()
        WHERE journey_id = #{journeyId}
    </update>

    <update id="removeJourneyByJourneyId" parameterType="map">
        UPDATE journey
        SET journey_delete_yn = 'Y'
        WHERE journey_id = #{journeyId}
    </update>


</mapper>